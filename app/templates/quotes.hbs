<h2>Quotes</h2>

<aside class="left-menu">
  {{#if session.isAuthenticated}}
    {{#link-to "quotes.new" class="button"}}Add New Quote{{/link-to}}
  {{/if}}
</aside>

<section class="main-container">
  <div class="main-col">
    {{outlet}}
  </div>
</section>
