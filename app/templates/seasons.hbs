
<div class="col-md-4">  
  <ul>
    {{#each model as |season|}}
      <li>{{link-to season.summary "seasons.season" season}}</li>
    {{/each}}
  </ul>
</div>
<div class="col-md-4">
  {{outlet}}
</div>
