<h2>Characters</h2>

<div class="row">
  <div class="col-md-6">
  <p>{{link-to "Add New Character" "characters.new"}}</p>
  <ul>
    {{#each model as |character| }}
    <li>{{link-to character.name "characters.character" character}}, {{link-to "Edit" "characters.edit" character}}</li>
    {{/each}}
  </ul>
  </div>
  <div class="col-md-6">
    {{outlet}}
  </div>
</div>